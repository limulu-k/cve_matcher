# 26.07.06

# 리뷰 목적

- **현재 진행 상황 보고**
    - 약 20940개의 cve-github link 매칭 db 생성 완료
    - codex 자동화를 통해 각 github link 접속하여 유효성 검증 및 product 여부 확인
- **문제 상황 공유**
    - nvd에 등록된 링크 들 중, **404 repo**가 존재
    - github 링크 중에 **redirect가 발생**하는 링크가 존재

# 프로젝트 진행 결과

- **이전 리뷰에서 현재까지의 진행 상황**
    - 기존에 **github link 유효성과, product 여부 판단에 정적 분석으로는 한계**가 ****있었음
    → codex를 통해 각 링크들 유효성 검증 및 nvd와 해당 정보로 product 판단 하도록 함
    - 이를 통해 최종적으로 **20940 개를 매치 시킨 db를 생성** 할 수 있었음
    - codex에서도 판단하기 애매한 것들을 걸러냄
        - github 링크 404 에러, 리다이렉션
        - 기존 정적분석 알고리즘 오류
        - non_product 의심 항목
        - 단일 cve에서 2개 이상의 github 링크 corss 매칭
- **현재 리뷰 받고 싶은 항목**
    - **github 404에러 발생 항목 처리**
        - 보통 ref내의 url은 commit링크인데 owner가 이를 삭제하는 경우가  **183개 존재**
        - [`github.com/owner/repo`로](http://github.com/owner/repo로) 주소를 변경하면 들어가지긴 함(링크가 유효함)
        - 하지만 패치 정보에 대한 유효성 검증이 애매해져서 accept 할지 고민이 됨
        
        ⇒ 일반 항목들과 동일하게 DB 등록
        
    - **github redirection 처리**
        - 명시된 url 접속시 다른 owner 혹은 repo로 redirection 되는 경우가 **785개 존재**
        - 케이스가 몇 개 나눠짐
            - repo 명이 다름
            - owner 명이 다름
            - repo&owner 명이 다름
        - 각 케이스를 어떻게 처리해야 할지 고민됨
        
        ⇒ git 레포 명 기준으로 등록
        
- **앞으로 해야 할 과제**
    - 그 이외의 케이스들을 전부 처리하였고, 편집이 용이하게 sqlite3로 db 구축이 끝났음
    - 말씀하신 github 링크 있는 cve를 1차적으로 필터링 하였고
    git 폴더 내 리스트로 2차 필터링, rule base로 accept, reject를 먼저 분리 후
    llm으로 필터링 검증 하였음
    - 기존 알고리즘으로 refiltering 한것들 중 명명된 이름 들이 유사도가 떨어져도 동일한 product를 가리킴을 발견(07.06) 다시 검사 후 재 등록 예정
    - 위 사항들 완료 될 시, DB 재생성 후, mysql로 db 변경하면 전체적인 요구사항이 충족 될 거 같음

- Tmp
    - review_inserted_manual_decision.csv 처리
    - mismatch_or_review_cve_github_pairs.csv 처리
    - metadata_nonproduct_term_boundary_audit.csv 처리